.admin.guests.index
  %h2= simple_format("All Guests")

  %table
    %thead
      %tr
        %td= 'Name'
        %td= 'Attending?'

    %tbody
      - guests.each do |guest|
        %tr
          %td= guest.name
          %td= guest.attending?
